<%= form_for(product) do |f| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :unit_price %>
    <%= f.text_field :unit_price %>
  </div>

  <div class="field">
    <%= f.label :sale_quantity %>
    <%= f.text_field :sale_quantity %>
  </div>

  <div class="field">
    <%= f.label :sale_price %>
    <%= f.text_field :sale_price %>
  </div>

  <div class="field">
    <%= f.label :on_sale %>
    <%= f.check_box :on_sale %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
